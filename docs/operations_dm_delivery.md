# 運用通知の金沢さんDM対応

2026-09-09。本人指定の通知先は `kanazawa@cnctor.jp` のSlack DM。
運用通知6箇所をIncoming Webhookから既存BotによるDMへ変更する。
本番配備・実投稿・同期実行・過去データの修復はこの作業に含めない。

## 通知先と失敗時の扱い

| 対象 | 送信先 | 失敗時 |
|---|---|---|
| 中優先度の日次通知 | 金沢さんDM | 例外を返し、送信済みを確定しない |
| 同期競合 | 金沢さんDM | 失敗を記録し、本処理を継続 |
| 案件ミラー警告 | 金沢さんDM＋既存の管理者全員 | 金沢さんDMの成功時だけ管理者経路から同じ宛先を除外 |
| 関連同期警告 | 金沢さんDM＋既存の管理者全員 | 同上 |
| 商談承認DMの失敗通知 | 金沢さんDM | 元の失敗結果を維持 |
| 暗号鍵自己診断の失敗通知 | 金沢さんDM | 診断結果を維持 |

高優先度インシデント、新規作成などの既存管理者全員通知は従来の対象を維持する。
金沢さんが管理者一覧にいない場合も、今回の運用通知は本人に送る。

```text
確定済みメール → Slack本人ID → 1対1のDM → 投稿受理を確認
                                             │
                            日次のみ → DBの送信済みを確定
```

## 共通送信処理

`src/notifications/operations_dm.py` はDB・業務モジュールを参照しない。
既存 `SLACK_BOT_TOKEN` と固定Slack API宛先を使用し、毎回メールから本人を解決する。
ユーザーIDやDM IDを推測して設定しない。HTTP200に加え、JSONの `ok` が真であることを
各段階で検査する。リダイレクトは追わず、例外・ログへ秘密値やSlack応答本文を出さない。
失敗は工程（設定・本人検索・DM開始・投稿）と許可済みの原因分類だけを記録する。
想定外の例外や改変された属性も出力時に再検査し、「その他」に置き換える。
投稿の自動再試行は行わない。各通信のタイムアウトは3秒で、全体の厳密な時間上限ではない。

必要な権限は `users:read.email`・`im:write`・`chat:write`。
根拠：[メールによる本人解決](https://docs.slack.dev/reference/methods/users.lookupByEmail/)、
[DMの開始](https://docs.slack.dev/reference/methods/conversations.open/)、
[投稿](https://docs.slack.dev/reference/methods/chat.postMessage/)。

## 2026-09-09の検証とレビュー

- 実装者の関連テスト286件成功。独立QAの最終全pytestは2,842件成功（28.43秒）、
  既存Starlette/httpx非推奨警告1件。
- 独立SEC・品質・QAの最終結果は、各BLOCKER 0・WARN 0。
  品質指摘の固定工程/原因分類と、高優先度即時通知に限る説明を反映済み。
- 使い捨てPostgres17・Unix socket接続と合成Slack応答で、共通送信から日次DBまで20項目成功。
  各3段階でHTTP失敗・JSON拒否・タイムアウト・転送拒否→rollback→次回commit、
  未設定/0件、同時2実行、53件の50＋3分割、API200/500を確認。
- 実Postgresの遅延制約でcommitだけ拒否し、未処理保持と次回再送時の重複を再現。
  SQL・共通DM・API経路は製品実装。Slackは合成応答であり実送達ではない。
- 環境変数を消去し、外部通信を拒否して検証。専用Postgres停止確認済み。
  一時証跡：`/private/tmp/dm-qa.70X4iJ/check.py`、`result-final.log`、`pytest-final.log`。
- 外部レビューは未実施。Gemini Pro・Claude Opus 5 中を新規チャットで選択確認済み。
  Geminiへのファイル添付は自動承認レビューが「具体的なコード全文と宛先への承認不足」
  として拒否。別方式への迂回はしていない。Claudeへの添付・送信も未実行。
- 承認対象資料：`/private/tmp/crm-operations-dm-review.txt`。
  製品差分・新規共通処理・既存日次DB処理・新規共通テスト・依頼文。
  メールは匿名化、鍵パターン0件。38,009文字、空白除外31,188文字、照合ハッシュ408066052。
  SHA256 `38bb11b955f7b4677aa2f691ce5bc9f21b3b9863d2f8d86698b60128ecb5a85f`。
  承認後に同一資料を両社へ添付し、内容照合→送信→採否整理を行う。

## 送達保証の範囲

SlackとDBは同時には確定できない。Slack受理後の応答喪失、DB確定失敗、プロセス停止では
次回の日次実行で重複する可能性がある。ミラー・関連同期も、運用DMの応答喪失後に
管理者経路が成功すると重複し得る。未送信を成功扱いにすることを避ける設計であり、
厳密な一度限りの送達保証ではない。

同じBotの認証失敗やSlack障害では、承認DM失敗の運用DMも届かない。
Slackとは別の経路での失敗・実行途絶監視は、既存の配備条件として引き続き必要。

## 配備前に残ること

ローカルのキーチェーン `slack-user-token` は取得結果44（項目なし）。
`config/.env` と `dashboard/.env.local` に `SLACK_BOT_TOKEN` は存在しなかった。
秘密値の表示・保存はしていない。実Slack本人IDの照合と、配備先Botによる本人解決・
DM権限・実送達は未検証。通知先の業務判断は確定済みなので再質問しない。

本番設定の変更・試験投稿は別指示後。Neon容量保証・自動監視も未完了のため、
このコード変更だけで同期APIを配備可とはしない。
詳細は [同期APIの配備条件](sync_api_deployment_readiness.md) を参照する。
