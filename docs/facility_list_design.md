# リスト作成マシーン（2026-09-11）

楽天トラベルに掲載されている宿泊施設から、条件に合う営業リストを作る機能。
`src/facility_list/` と `dashboard/app/(dashboard)/facility-list/` が実体。

```
   ① 取り込み（バッチ）        楽天トラベルの公開ページ ──▶ RakutenFacility
        scripts/import_rakuten_facilities.py

   ② 抽出（画面）              条件で絞る ──▶ CRMと突合 ──▶ CSV
        /facility-list  →  POST /api/facility-list/{preview,export}
```

画面から楽天へはアクセスしない。先に溜めた母集団を絞るだけなので即座に結果が出る。

---

## 1. なぜ API ではなく公開ページなのか

楽天ウェブサービス（楽天トラベル施設検索API）を使えば客室数・クチコミ点数・電話番号まで
1リクエスト30件で取れる。**それでも使わない。**

```
   利用規約 第10条(4)  楽天アフィリエイト以外の方法で収入を得ることを禁止
   利用規約 第10条(7)  取得した情報は、会社が指定した目的・方法でのみ利用可能
```

自社商材の営業リスト作成がこれに触れる可能性があり、2026-09-11に本人判断で
**公開ページ方式**を採用した。公開ページの取得は robots.txt が許可している範囲で行う。

| | API方式 | 公開ページ方式（採用） |
|---|---|---|
| 施設一覧 | 1リクエスト30件 | エリア一覧が1リクエスト30件（同じ） |
| 客室数・点数 | 一覧で取れる | 一覧で点数、施設ページで客室数 |
| 電話番号 | `telephoneNo` で取れる | **取れない**（CRM側から補う） |
| 規約 | 第10条(4)(7)に触れうる | robots.txt の範囲で許可 |

将来 API 利用の可否を楽天へ確認して OK が出れば、`rakuten_client.py` を差し替えるだけで
移行できる（パーサとドメインはそのまま使える）。

## 2. 実測で確認したこと（2026-09-11）

すべて実ページを叩いて確かめた。**推測ではない。**

| 確認したこと | 結果 |
|---|---|
| 掲載施設の総数 | サイトマップに **43,753施設**（`hotelinfo/plan/{no}`） |
| 都道府県で絞れるか | `hotellist/area/{スラッグ}/?f_page=N` で30件ずつ。47県分のスラッグを実取得 |
| 一覧から取れる項目 | 施設名・クチコミ点数・郵便番号＋住所・最安料金（鳥取県30件で欠損0） |
| 客室数 | 詳細ページ `{no}_std.html` の「総部屋数」（1008=471室、168000=9室で確認） |
| クチコミ点数・件数 | 施設トップの `property="ratingValue"` / `property="reviewCount"` |
| 写真枚数 | 施設トップの「写真・動画(83)」 |
| **カスタマイズページの有無** | 施設トップに `GW{施設番号}{連番}.html` へのリンクがあれば公開中 |
| カスタマイズページの実勢 | 大阪市30施設中 **18施設が公開あり・12施設が未作成** |
| robots.txt | `/HOTEL/{一部の施設番号}/*` を名指しで禁止（実測19件）。実装で除外している |

### 取れないもの

| 項目 | 状況 |
|---|---|
| 施設カテゴリー（ホテル/旅館/ペンション） | **楽天の公開ページに項目が存在しない**。検索UIの「宿タイプ」も温泉宿/高級宿の2つだけ。施設名と設備からの**推定**で出し、確からしさ（高/中/低）を併記する |
| チェックイン機の導入 | 施設ページには**ほぼ書かれていない**（鳥取県40施設で実測し、記載ありは0件）。WEB検索での二次判定を別に用意した（下記） |
| 電話番号 | 公開ページに出ない。CRMに取引先があればそこから埋める |

## 3. チェックイン機の判定（2段構え）

メイリーのQRチェックインは**既存のチェックイン機をそのまま活かす**設計なので、
「チェックイン機があるか」が提案可否をそのまま決める。ところが楽天トラベルの施設ページには
ほとんど書かれていない（**鳥取県40施設で実測して記載ありは0件**）。そこで2段構えにしている。

```
   一次判定  施設ページの記述        無料・全件      services.detect_check_in_machine()
   二次判定  WEB検索で確認           有料・候補のみ   check_in_machine_search.py
```

- 二次判定は `claude-haiku-4-5` ＋ web_search を1コール。軽い定型判定なのでHaiku
- 判定は `yes` / `no` / `unknown` の3値。**「書かれていない」は `no` ではなく `unknown`**
- `yes` / `no` には必ず根拠テキストとURLを残す。**根拠が無い断定は `unknown` に倒す**
- 結果は `FacilityCheckInMachine` テーブルに残り、施設マスタを取り込み直しても消えない
- `ANTHROPIC_API_KEY` が無ければ検索せず「未実行」を返す（例外にはしない）

```bash
# まず対象件数を見る（APIを呼ばない）
python scripts/search_check_in_machines.py --prefecture 鳥取県 --dry-run

# 実際に検索する（1件ごとにClaude APIを呼ぶ。既定の上限は50件）
python scripts/search_check_in_machines.py --prefecture 鳥取県 --limit 20
```

## 4. 構成（DDD）

```
   src/facility_list/
   ├── domain/          何にも依存しない。DBも楽天も起動せずテストできる
   │   ├── models.py    Facility / ListCriteria / RoomCountRange / 各種状態
   │   └── services.py  カテゴリ推定・条件判定・商材適合・チェックイン機の一次判定
   ├── application/
   │   ├── import_facilities.py  取り込み（浅い→深い の2段階）
   │   └── build_list.py         抽出 → CRM突合 → 行を組む
   ├── infrastructure/
   │   ├── rakuten_client.py       HTTP取得・robots・レート制御・サイトマップ
   │   ├── rakuten_page_parser.py  HTML → 事実（HTTPを持たない純粋関数）
   │   ├── crm_matcher.py          ClientNameIndex と Notion で突合
   │   ├── db.py                   RakutenFacility の読み書き（psycopg直接）
   │   └── exporters.py            CSV・スプレッドシート用の2次元配列
   └── presentation/   画面は dashboard 側。ここはCLIのみ
```

判定ロジックは全て Python 側にあり、画面（Next.js）は FastAPI 経由で呼ぶだけ。
TypeScript 側に業務ルールを二重実装しない。

## 5. 抽出条件

| 条件 | 指定の形 | 備考 |
|---|---|---|
| 都道府県 | 複数選択 | 未選択なら全国（取り込み済みの分だけ） |
| 客室数 | ◯室以上◯室未満 | 既定は1室以上・上限なし。**客室数が不明な施設は含めない** |
| クチコミ点数 | ◯.◯以上◯.◯未満 | 「クチコミなしも含める」を別に持つ |
| クチコミ件数 | ◯件以上 | 母数が少ない点数を信用しないため |
| 施設カテゴリー | 複数選択 | 推定。確からしさを列に出す |
| 楽天カスタマイズページ | 未作成のみ / 公開ありのみ / 問わない | |
| 温泉 | あり / なし / 問わない | |
| 掲載写真 | ◯枚未満 | 撮影提案の対象 |
| チェックイン機 | 導入あり / 未確認 / 導入なし | WEB検索で確かめた施設だけが「あり／なし」になる |
| チェーン系を除く | on/off | アパ・東横イン・ルートイン等（`CHAIN_NAME_PATTERNS`） |
| CRMの取引状態 | 未取引のみ / 既存のみ / 問わない | **候補が複数の施設は「未取引」に混ぜない** |
| 提案済みサービスを除く | 商材を複数選択 | Notion「提案済みサービス」と突合 |

## 6. 出力（列は本人指定・2026-09-11）

そのまま架電管理表として使える形にしてある。左が営業の記入欄、右が機械が埋めた事実。

```
   施設名 | コールステータス | 担当 | 電話番号 | メモ | 姿勢 | 顕在課題 | 現状の取引
   | URL | 施設カテゴリー | 客室数 | 最低料金 | エリア | 都道府県 | チェーン
   | 楽天口コミ点数 | 楽天口コミ数 | FAX番号 | メール | 作成者 | 作成日時 | 日付
   〔以降は判断の根拠〕 カテゴリー推定の確からしさ / 楽天カスタマイズページ / 温泉
   / 写真枚数 / チェックイン機 / CRM状態 / 取引先名 / 提案済みサービス
   / 担当者名 / 役職 / 提案候補商材
```

- `コールステータス` `メモ` `姿勢` `日付` は**空欄で出す**（営業が埋める）
- `顕在課題` だけは機械で埋める。「クチコミ3.9（1,579件）と伸びしろあり」のように
  **事実だけ**を並べる（推測は書かない）。架電の一言目がそのまま決まる
- CSVは**BOM付きUTF-8**。付けないとExcelが文字化けする

## 7. 商材の当てはめ

施設データから確実に言えることだけを根拠にする（`suggest_products()`）。

| 施設の状態 | 提案候補 |
|---|---|
| 楽天カスタマイズページ未作成 | WEB制作（楽天CP）・クリエイティブラボ |
| クチコミ4.0未満 かつ 50件以上 | フルスコ・ホテルラボ レビュー |
| チェックイン機あり（確認済み） | メイリー（QRチェックイン） |
| 客室数 | リピッテホテル（**客室数から月額が決まる**ので提案額まで出す） |
| 客室50室以上 | ホテルラボRM |
| 温泉あり | ホテルラボ |
| 掲載写真20枚未満 | ホテルラボ（OTA写真撮影） |
| 館内にレストラン | セルフオーダー |

## 8. 運用

```bash
# まず1県・30件で試す（DBに書かない）
python scripts/import_rakuten_facilities.py --prefecture 鳥取県 --max-pages 1 --dry-run

# 1県を丸ごと取り込む
python scripts/import_rakuten_facilities.py --prefecture 鳥取県

# 一覧だけ全県（詳細は後で）
python scripts/import_rakuten_facilities.py --all-prefectures --shallow-only
```

- **全国を一度に回さない。** 1施設あたり2リクエスト・既定1.0秒間隔なので、
  43,753施設の詳細取得は24時間規模になる。都道府県単位で進める
- `--interval` を短くしすぎない。429（レート制限）を踏むと専用の例外で止まる
- 掲載終了した施設は**行を消さず** `isListed=false` にする
  （ProjectMirror全消失インシデントの教訓。母集団は消すより残す方が安い）

### rc=0 を信用しない

取り込み結果は「成功件数」だけでなく次を必ず出す。

```
   詳細取得: 成功N件 / 掲載終了N件 / 失敗N件 / 客室数が読めなかったN件
```

**客室数が読めなかった割合が3割を超えたらエラーログを出す**（`ImportResult.is_healthy`）。
楽天側のHTML変更でパーサが静かに壊れても、件数だけは出てしまうため。
失敗が1件でもあれば終了コードを2にする。

## 9. レビューで直したこと（2026-09-11）

動物チーム3体（shirokuma-sec / obasan-quality / kuma-qa）を並列で回し、**BLOCKER 2件**を
含む指摘を反映した。どれも「テストは全部通っているのに壊れている」類だった。

| 重要度 | 指摘 | 対応 |
|---|---|---|
| BLOCKER | 「未取引のみ」を選ぶと書き出しが**必ず500エラー**。件数チェックが`build_list()`を使っており、CRM条件のガードに自分で引っかかっていた | 件数だけ数える`count_candidates()`を分けた。リグレッションテストつき |
| BLOCKER | CRM突合に失敗した施設を`NOT_FOUND`（＝未取引）として返していた。Notionが一時的に落ちただけで既存顧客が新規開拓リストに混ざる | `NOT_CHECKED`に変更。突合できなかった件数を警告ログに出す |
| WARN | パーサが全滅すると`is_healthy`が分母0で「健全」に倒れ、生きている施設が一括で`isListed=false`にされうる | 掲載終了の割合も見るようにし、異常時は掲載終了の印付けを**保留**する |
| WARN | CRM突合が1件ずつ新しいDB接続を開く（500件で最大1,500接続） | 名前の候補を1回のクエリでまとめて引くようにした |
| WARN | CSVの数式インジェクション（施設名が外部サイト由来） | `=` `+` `-` `@` で始まるセルをエスケープ |
| WARN | `FacilityListRun`（履歴）がテーブルだけで未使用 | 書き出し時に条件と件数を記録するようにした |
| WARN | プレビュー表が`slice(0,17)`で列を切っており、案内文で名指しした「CRM状態」が画面に出ていなかった | 出す列をバックエンドが名前で決めるようにした |
| INFO | 楽天の404ページは空ではなく**1,822バイトのHTML**。本文の長さで判定していた | ステータスコードで判定するよう修正 |
| INFO | robots.txtが施設ページ全体を禁止に変えたら「全部許可」に倒れる | 広い禁止指定を検出して全面停止するようにした |

テストは 67件 → **126件**に増やした（APIルート・取り込みバッチ・DB層は当初1件も無かった）。

## 10. 他社モデルレビューで直したこと（2026-09-12）

ChatGPT（GPT-5.6 Sol・思考量中）と Gemini（3.1 Pro）に主要4ファイルを渡した。
**両社が独立に同じBLOCKER**を挙げた（CLAUDE.md「複数のレビュアーが独立に同じ指摘を
出したものは、ほぼ本物」）。

| 重要度 | 指摘（出どころ） | 対応 |
|---|---|---|
| BLOCKER | **施設名 ≠ CRMの会社名の既存顧客が「未取引」になる。** CRMには運営会社名で載っていることが多く、名前が違うだけの既存顧客に新規営業をかける（ChatGPT） | `NOT_FOUND` を廃し `NO_NAME_MATCH`（名前照合で当たらなかった）に。CSVは「未取引の可能性（名前照合のみ）」と表示。画面にも注意書き |
| BLOCKER | **弱い名前候補で誤マッチし、別会社の担当者名・メール・電話が別施設の行に出る。** 「ホテルABC 大阪」の「大阪」が1件当たると確定していた（ChatGPT） | 名前候補に強さ（EXACT / STRIPPED / WEAK）を持たせ、WEAK は都道府県の一致を必須に |
| BLOCKER | **「500件までなら300秒に収まる」に根拠が無い。** 全件が既存顧客ならNotionを500回読む（**ChatGPT・Gemini 両方**） | 件数ではなく**時間予算**（既定180秒）で制御。使い切った分は `NOT_CHECKED` として正直に返し、画面にも件数を出す |
| BLOCKER | **CRMミラーが古い・空でも「正常に0件」なので例外にならず、既存顧客が丸ごと新規リストに載る**（ChatGPT） | 書き出し前に `ClientNameIndex` の件数と最終同期を確認し、古ければ 503 |
| BLOCKER | **`user_id` が空でも通り、監査ログ無しで個人情報CSVを出せた**（ChatGPT） | `user_id` を必須化。履歴を残せなければ書き出しごと中止 |
| BLOCKER | **robots.txt の解釈が不十分。** UA行が連続すると `*` を読み落とす／ワイルドカード規則を無視／サイトマップとエリア一覧は未評価（**ChatGPT・Gemini 両方**） | 施設番号ベースを捨て、**URLパスベースの評価器**に置き換え。Allow対応・`*`/`$`対応・ホストごとに取得し、**全リクエストで評価** |
| WARN | 「同時3」が定数だけで保証されていない（ChatGPT） | `Semaphore` を追加 |
| WARN | `time.sleep()` をロックの中で呼んでいる（Gemini） | ロック内では順番を決めるだけにし、待つのは外へ |
| WARN | 429で全ジョブが即失敗。`Retry-After` を無視（ChatGPT） | `RakutenRateLimitedError` に `Retry-After` を持たせた |
| WARN | リダイレクト先を再検証していない（ChatGPT） | 最終URLのホストとrobotsを確認 |
| WARN | **エリア一覧の部分的パース失敗を「最終ページ」と誤認して母集団が静かに欠ける**（ChatGPT） | カード数と読み取れた件数を突き合わせ、打ち切り判定はカード数で行う |
| WARN | 先頭修飾語の除去が1回だけ（Gemini） | 落とせなくなるまで繰り返す |
| WARN | `limit` 指定時も全件を突合していた（Gemini） | CRM条件が無ければ突合前に切る |

テストは 126件 → **158件**（全体 3,318件）。

## 11. 本番へ出す手順（2026-09-12 調査）

**Vercelのプロジェクトは2つあり、順番を間違えると壊れる。**

```
   crm-sfa-integration            ルート。FastAPI(api/index.py)。APIエンドポイント
   crm-sfa-integration-dashboard  dashboard/。Next.jsの画面
                                  ★ build に `prisma migrate deploy` が入っている
                                  ＝ここをデプロイするとマイグレーションが自動で当たる
```

どちらも**Git連携が無い**ので、`git push` だけでは本番は変わらない
（[[feedback_vercel_no_git_integration_manual_deploy]]）。毎回 `vercel --prod` が要る。

```
   ① cd dashboard && vercel --prod
        → マイグレーション 20260911000000_add_facility_list が本番Neonへ当たる
        → RakutenFacility / FacilityCheckInMachine / FacilityListRun が作られる
        → 画面 /facility-list とサイドバーの「リスト作成」も出る
        ※ この時点ではAPIが無いので、画面で操作すると失敗する（数分の不整合）

   ② cd .. && vercel --prod
        → /api/facility-list/{preview,export} が公開される
        → ここで初めて画面が動く

   ③ 母集団を入れる（画面から操作しても何も出ない。先に取り込みが要る）
        DATABASE_URL=<本番> python scripts/import_rakuten_facilities.py --prefecture 鳥取県
```

**①を先にやる。** 逆にすると、APIがまだ存在しないテーブルを触りに行く。

### マイグレーションの事前検証（2026-09-12 実施済み）

手書きのマイグレーションSQLを、使い捨てのローカルPostgres(17)へ実際に流して確かめた。

```
   initdb → pg_ctl start → createdb → prisma migrate deploy
   → All migrations have been successfully applied.

   prisma migrate diff --from-config-datasource --to-schema prisma/schema.prisma
   → No difference detected.
```

**手書きSQLとPrismaスキーマの一致をツールで確認できた**（社内レビューで「目視のみ・
ツール照合は未実行」として残っていた点）。

### ローカル実Postgresでのエンドツーエンド検証（2026-09-12 実施）

使い捨てのPostgres 17を立てて、本番に一切触らずに通した。

| 確かめたこと | 結果 |
|---|---|
| 全マイグレーション適用 | 成功（手書きSQLを含む） |
| Prismaスキーマとの一致 | `No difference detected.` |
| 鳥取県30施設の取り込み | 30件書き込み。客室数の欠損0・警告0・失敗0 |
| 実DBからの読み出し | 30件。客室数・点数・最安料金・温泉・カスタムページすべて復元 |
| 条件で絞る | カスタマイズページ未作成 × 30室以上 × チェーン除外 → **10件** |
| CSV生成 | BOM付き11行。「顕在課題」も自動で入る |
| API `/preview` | HTTP 200、10件、16列 |
| 認可（トークン無し） | HTTP 401 |
| 認可（`user_id`無しの書き出し） | HTTP 422 |
| **CRMミラーが空のまま「未取引のみ」** | **HTTP 503**「同期が古いため使えません」＝他社レビューで入れた安全装置が実際に効いた |

## 12. 未解決・保留

| | 状況 |
|---|---|
| 本番配備 | 旧版 `24945d1` は2026-09-12配備済み（Vault記録）。本節以降の二次照合変更は未配備 |
| **CRM突合の精度** | 住所・電話の二次照合を実装（§13）。本番への反映と追加項目の全件取り込みは未実施。実際の一致率は未計測。住所単独の運営会社候補は要確認として新規から除外する |
| 時間予算の実測 | 既定180秒が妥当かは**未検証**。Notionの実応答時間を測っていない |
| カテゴリー推定の精度 | 「皆生グランドホテル天水」（実態は旅館）が hotel 判定になるなど誤りが残る。確からしさ「中」で出している |
| スプレッドシート出力 | 2次元配列を作るところまで。既存の `spreadsheet_client.py` への接続は未実装（今はCSVのみ） |
| 画面の手動確認 | **ブラウザでの操作は未実施。** ログイン(2FA)の壁があるため、ローカルでは本番と同じ `npm run build` が通ることまでを確認した |
| チェックイン機のWEB検索 | 実装済みだが**実APIでの実行は未検証**（`ANTHROPIC_API_KEY`が未設定）。精度とコストは実測していない |
| 抽出条件の定義が4箇所 | `ListCriteria`(Python) / `ListCriteriaRequest`(Pydantic) / `normalizeCriteria`(TS) / 画面の`Criteria`型。条件を1つ足すと4ファイルを触る（obasan-quality指摘）。既存画面と同じ構成なので今回は踏襲したが、条件が増え続けるなら共通化を検討する |
| ~~マイグレーションのツール照合~~ | **2026-09-12に完了。`No difference detected.`**（§11） |


## 13. CRM住所・電話の二次照合（2026-09-12 主機CX）

**本番の名前だけの照合に対する追加実装。未配備・全件取り込み未実施。**

```
既存Notion同期 ──▶ ClientNameIndex（名前＋住所・電話キー）
                               │ 一括検索
施設の名前・住所・電話 ──────────┤
                               ├─ 単独住所／電話候補 ──▶ 要確認
                               ├─ 複数社／値の矛盾   ──▶ 要確認
                               ├─ 複数根拠で1社      ──▶ Notion現行値を確認
                               └─ 一致なし           ──▶ 完全取得時のみ新規候補
```

### 判定と情報開示

| 条件 | 結果 | 新規のみ | CRMの担当者連絡先 |
|---|---|---|---|
| 名前が違い、住所だけ又は電話だけが一致 | AMBIGUOUS（候補1社でも要確認） | 除外 | 出さない |
| 住所＋電話が1社に一致 | 現行値にも矛盾がなければMATCHED | 除外 | 確定時のみ |
| 名前候補＋住所又は電話が一致 | 現行値にも矛盾がなければMATCHED。WEAKの都道府県一致も必須 | 除外 | 確定時のみ |
| 名前・住所・電話が異なる会社を指す／同じ住所に複数社 | AMBIGUOUS | 除外 | 出さない |
| 名前だけの既存一致 | 従来の名前判定を維持。WEAKの裏付け不足は要確認 | 除外 | 確定時のみ |
| 一致なし・二次項目の全行取得完了・ミラーが新鮮・施設側に有効な住所又は電話あり | NO_NAME_MATCH（旧APIの値名を維持） | 含める | 出さない |
| 取り込み途中・古い・DB/API取得失敗・施設側の材料なし・時間切れ | NOT_CHECKED | 除外 | 出さない |

- CRMに会社の本社所在地しかなく、施設の所在地が無い場合は依然取りこぼす。「未取引」と断定しない。
- **楽天の現在の取り込みには電話番号がない。** 実データでは住所単独候補を新規から除外する効果が中心。電話照合はFacility.telephoneに信頼できる値がある場合だけ働き、電話を取りに行く機能は今回追加しない。
- CSV末尾に「CRM照合根拠」を追加。候補が別会社に分かれる場合は会社別に根拠を出す。
- `excluded_ambiguous_count`は、要確認のためCRM条件で除外した件数。出力に残った`ambiguous_count`と分離し、画面に表示する。
- `unchecked_count`は、時間切れだけでなく取得失敗・同期途中・材料不足も含む。新規のみで全件が落ちても0件成功だけの表示にしない。

### 保存・表記ゆれ・時間制限

- `ClientNameIndex`へnullableな`normalizedAddress`、`normalizedPhone`、`identitySyncedAt`を追加。名前検索の返り値は維持する。
- Webhookと通常の全件巡回が既に取得しているNotionプロパティを保存する。**書き出し時に全取引先をNotionへ検索する方式は見送った**（10万件規模と180秒の予算が合わないため）。
- 住所・都道府県・TELの3プロパティが期待した型で届いた行のみ取得済みにする。値が空でも取得済み。プロパティ欠落・型変更は未取得扱いで、新規判定を開放しない。
- 住所はNFKC・空白・数字間のハイフン・数字表記の丁目/番/号を揃える。建物名・階・部屋は削らない。漢数字の番地、旧字体、建物名の有無などは今回の正規化対象外。市区町村までの短い住所は照合キーにしない。
- 電話は全角・括弧・ハイフン・+81を揃える。国内10/11桁だけを受理し、内線・複数番号・説明付きは推測しない。
- 一括取得は同一のDB時点で候補と未取得件数を読む。未取得が1行でもあれば不一致を確定しない。古いミラーの二次候補は使わない。
- 時間予算は180秒のまま。単発・一括のDB検索、Notion通信の前後にも予算を確認。名前・二次キーSQLは5秒の待機上限、接続は既存の10秒上限。Notionは残り時間/10秒の小さい方を通信待ち上限にして、この用途の自動再試行を0回にする。
- 連絡先は確定した取引先だけを条件付きでページングし、同じ実行内で再利用する。全連絡先DBの先行一括取得を廃止。
- 通信の待ち上限は実行全体を強制停止するものではなく、進行中の1通信分の超過はあり得る。実Notionの応答時間と180秒の妥当性は未計測。

### 同期時刻の補正（既存説明の訂正）

`src/relation_sync/sync.py`と`sync_cursor.py`の実コードを確認すると、通常同期は
**created_timeで分割して全件を巡回する方式**だった。以前の「更新されたページだけの差分同期」という説明は誤り。
`ClientNameIndex.syncedAt`は一巡の基準時刻で、最終実行時刻とは異なるため、鮮度は引き続き`SyncCursor.updatedAt`を見る。

一巡成功時には再開用の`client_name_index`行を消す。完了直後に鮮度が「不明」とならないよう、
**成功した場合だけ**`client_name_index_completed`に完了時刻を残す。鮮度には2行の`updatedAt`の最新値を使う。
既存の再開位置・削除判定・ロックは変更しない。途中取得や急減検知を成功記録にしない。

### 配備・取り込みの手順（未実施）

1. 他社レビューを終え、本人の配備指示後、従来どおりdashboard→APIの順で反映する。新列はnullableなので、間の旧APIのINSERTも動く。
2. 既存の分割巡回・Webhookで二次項目を取り込む。配備時点より前の区間は次の一巡で入るため、**配備だけで新規判定が開放されるわけではない**。
3. 未取得件数が0・最後の同期実行が48時間以内であることを実件数で確認する。住所/TELが空の件数も分けて把握する。
4. 本人の指示した環境・範囲で、住所候補が新規のみから除かれること、CSV根拠と除外件数を確認する。

読み取りの確認SQL（接続先を確認して使う）：

```sql
SELECT COUNT(*) AS total,
       COUNT("identitySyncedAt") AS checked,
       COUNT(*) FILTER (WHERE "identitySyncedAt" IS NULL) AS pending,
       COUNT("normalizedAddress") AS address_keys,
       COUNT("normalizedPhone") AS phone_keys
FROM "ClientNameIndex";
SELECT name, "updatedAt" FROM "SyncCursor"
WHERE name IN ('client_name_index', 'client_name_index_completed');
```

- 本番の取り込み速度・追加項目の充足率は未計測。既存の全件バックフィルは`syncedAt`更新と削除も行うため、**分割同期途中に実行しない**。専用の高速バックフィルは今回は追加せず、必要なら別イシューにする。
- 旧APIへ戻すと二次項目は更新されなくなる。新コードへ再度進める際は二次項目を再取得してから開放する。既存全件同期を勝手に起動したり、既存しおりを消して取り込みを早めたりしない。
- 本番配備・本番マイグレーション・本番データ変更は今回行っていない。

### レビュー・検証

- 独立SECで見つかった「ミラーの電話が空、現行Notion電話が施設と異なるのに確定する」経路を修正。採用根拠の項目以外にも現行値の矛盾を確認する。
- 予算切れ後の名前検索、単独候補の表示、除外した要確認件数、会社別の照合根拠を修正。
- 独立QAが使い捨てPostgres17へ全30マイグレーションを適用し、Prismaスキーマとの違い0を確認。合成506行で保存・検索・未取得ガード・電話削除・バッチ境界を確認し、同期完了時刻の追加検証を含め計24シナリオ成功。Notion・通知は代替応答。
- Python全件は3,355成功・12スキップ・環境制約2失敗。macOSプロセス検査の2失敗は権限付きで再実行して2成功（計3,357件の成功確認）。関連261件成功、TypeScript型検査成功、Vitest289件成功。独立SEC/品質/QAは最終BLOCKER 0・WARN 0。
- **Gemini Pro / Claude Opus・中のレビューは未実施。送信は本人が「いいよ」と承認済み。** 再開時にChrome接続が切れており、利用可能ブラウザは0。指定Profile 18の拡張有効・接続設定正常は診断済み。本人が復旧用ウィンドウ起動も承認し、Profile 18に新規ウィンドウを開く処理は成功した。ただしChrome再選択は利用不能・ブラウザ一覧0件のままで再接続できない。Chromeプラグインの再接続/再インストール待ち。差分は添付・送信とも未実施。送信・ウィンドウ起動の許可を改めて聞く必要はない。
- 本番の一致率、Notionの実応答、ブラウザでの施設リスト操作は未検証。
