# 古いWebhookによる巻き戻り防止

2026-09-08。対象は同期ディスパッチャとシート行再試行（outbox）。本番への適用・本番操作は未実施。

## 変更前と変更後

以前はmappingの最終同期時刻を読む瞬間だけ古さを判定していた。別ワーカーが先に新しい値を書いた後でも、先に判定を通った古い処理が後から上書きできた。シート行再試行にも、Notion取得と行追加の間に同じ競合があった。

現在は同一DB・Notionキーの同期を1件ずつ処理する。

```
専用ロック取得 → mapping再読込 → 古さ判定 → 現在値取得・判定
 → 受理時刻の永続保存 → 外部書込 → 完了時刻保存 → mapping時刻保存
 → 接続終了・ロック解放
```

`RecordSyncWatermark` の受理時刻（acceptedAt）は外部書込の直前に先行保存する。外部書込後に完了時刻の保存が失敗しても、それより古いイベントは拒否する。未完了の同時刻イベントは再送可能。完了時刻（completedAt）とmappingの最終同期時刻のうち新しい方以下は、従来どおり `stale_event` とする。Notion APIの読み取りが古い値を返しても、Postgresに残した時刻が判断の下限になる。時刻は小数秒6桁を保存する。

ロックはPostgresのセッション単位で保持する。受理時刻保存の確定後も外部書込完了まで解放しない。専用namespaceは `record_sync`、既存のシート行作成ロックより常に外側で取得する。競合は `RecordSyncBusy` を送出し、Webhookの既存エラー経路でHTTP500・再送にする。outboxの競合は試行回数を消費せず差し戻す。

outboxはNotion取得から行追加・行番号保存まで同じロックを保持する。手動復旧の棚卸しで行番号を記録するときも同じロックを取得する。新規ページ作成は従来のmapping登録を先に済ませ、その後の行追加だけ同じロックで保護する。登録後に新しい同期が先行した場合、古い作成時プロパティを捨ててoutboxへ渡す。ロック取得失敗も既存の行作成失敗通知・再試行へ渡す。

## 配備条件

- `20260908000000_add_record_sync_watermark` の追加マイグレーションを先に適用する。既存データの削除・書き換えはない。
- `DATABASE_URL_UNPOOLED` に直接接続を設定する。未設定・既知の `-pooler` ホストは例外にして処理を止める。独自の接続プール名は自動判定できないため、運用者が直接接続を確認する。
- テーブル未配備・DB接続不可・受理時刻保存不可の場合、既知レコードの外部更新を開始しない。
- このテーブルは再送に対する記録なので自動削除しない。将来削除するときは古い再送を受け付ける期間を別途決める。

## 保証の範囲と残る制約

- 本番のプロセス間排他はPostgresを使用する。SQLite実装だけは同一store個体・単一プロセスのテスト用ロックを使う。同一SQLiteファイルを別storeや別プロセスで開く使い方は保証しない。
- ロックに参加しない手動編集・バックフィル・別システムの書込は対象外。Notion webhook後段のカレンダー同期等にも適用していない。
- 外部書込とPostgresは同一トランザクションにならない。書込後に失敗した同時刻イベントの再送は外部書込を繰り返す可能性がある。接続断でロックを失った実行中リクエストの停止や、外部APIの読み取り整合性は保証しない。
- 時刻は従来どおりレコード単位で比較する。別項目の古い差分・同時刻の別イベント・ツール間の時計ずれを項目単位で解決する変更ではない。部分的な書込スキップ時にも完了時刻を進める既存仕様は維持する。
- 新規ページ自体の二重作成排他は今回の対象外。mapping登録前の既存の作成・補償経路は維持する。登録後のロック失敗でoutboxにも積めなかった場合は既存通知経路に従い手動復旧が必要。

## 検証

単体テストは並行dispatchの競合と再送、古いmapping読取、完了印保存失敗後の古い更新拒否・同時刻再送、受理保存失敗時の書込ゼロ、接続未設定/pooled拒否、新規登録直後の新しい更新、outbox競合差し戻しを確認する。外部ツールは偽物であり、本番の実動作確認を意味しない。実Postgresの独立検証結果はレビュー後に追記する。

独立QAでは専用ローカルPostgresを使用し、製品SQL11項目、並行dispatch3項目、Notion競合時のHTTP500・イベント取得印解除・同じイベント再送HTTP200、outbox差し戻しと試行回数1→0を確認した。修正前の全体テストは2,767件成功。証跡は `/private/tmp/crm-freshness-qa.cefgeV`（ローカル一時保存）。実外部API・本番は未検証。

セッション単位のロックがトランザクションの確定後も維持される仕様は [PostgreSQL 17公式ドキュメント](https://www.postgresql.org/docs/17/explicit-locking.html#ADVISORY-LOCKS) を参照。

## 最終独立検証とレビュー状況

- 2026-09-08 主機CX：修正後の全pytestは2,770件成功（26.26秒）。既存のhttpx非推奨警告1件。Prisma validateも成功。本番の.envは読み込まず検証した。
- 専用Postgres17で18項目成功。受理時刻の先行確定後も2接続目を排他、マイクロ秒精度、古い更新拒否・未完了同時刻再送、DB障害時書込ゼロ、例外後解放を確認。
- 製品dispatcherと実Postgresで並行更新を確認。Notion受信処理はロック競合時500・イベント重複印の解放・同一イベント再送200を確認。outboxは製品SQLで競合時の差し戻しと試行回数1→0、新規mapping保持・シート書込ゼロ・pending登録を確認。
- 外部APIと通知は偽物。実Neon・接続プール・本番配備・HTTP受信監査ログのDB書込は未検証。専用クラスタは停止確認済み。証跡は `/private/tmp/crm-freshness-qa.cefgeV/`（一時ファイルのため永続保証なし）。
- 独立品質レビューのWARN1件を修正：古い新規スナップショットを捨てる際、再試行キューへの登録に失敗しても既存Slack通知で手動復旧を案内する。登録成否×行作成有効/無効の4ケースを回帰テスト化。独立SEC再レビュー・品質再レビュー・QAはいずれもBLOCKER 0 / WARN 0。
- 本人の「いいよ」で送信承認を取得し、Gemini Pro / Claude Opus 5（高）へ同一本文を送信。空白除外20,204文字・ハッシュ1195825170が両社で一致。Claudeは添付プレビューの全文で照合した。初回入力は自動承認で拒否されたが、本人承認後に正常送信・回答取得まで完了。本番操作は別の指示が必要。

## 配備前の接続・未完了確認

outboxはキュー取得前に直接接続の設定、時刻テーブルと必要列の存在、SELECT・INSERT・UPDATE権限、読み取り専用接続でないことを確認する。共通設定の不備ならキューの試行回数を増やさず異常終了する。検査は読取のみで、検査後のDB障害まで防ぐものではない。ホストは接続文字列に明示する（Unixソケットの明示も可）。省略してPGHOST等から暗黙に引く設定は拒否する。

直接接続の必要数は「同時実行するレコード同期・outbox修復の数＋同時に行作成ロックを使う数」。これに事前検査用の短い接続と他用途分が加わる。配備前にDBの接続上限・予約枠とアプリの同時実行数を照合する。接続上限の変更や本番測定は未実施。

受理済み・未完了が続くレコードは、次の読み取りで件数を確認できる。猶予は暫定15分で、配備時に通常処理時間・送信元の再送間隔と照合する。古いイベント発生時刻が基準のため、到着直後の遅延イベントも候補になる。2回以上の観測で同じ対象が残り、処理中でないことを確認してから調査する。これは調査用SQLであり、自動監視・通知は未実装。

```sql
SELECT "dbKey", COUNT(*) AS pending_count
FROM "RecordSyncWatermark"
WHERE ("completedAt" IS NULL OR "acceptedAt" > "completedAt")
  AND "acceptedAt" < CURRENT_TIMESTAMP - INTERVAL '15 minutes'
GROUP BY "dbKey";
```

未完了を一括削除・完了扱いにはしない。再送や外部反映状況を照合してから対応を判断する。新規行追加の失敗ログは競合・設定・DB・その他の固定分類だけを記録し、接続文字列を含みうる例外本文は出さない。

## 他社レビューの採否

- [Gemini Pro](https://gemini.google.com/app/efc8fe30a2befe66?hl=ja)：BLOCKER 0、WARN 3。新規行追加の原因記録と接続数条件を採用。取得行の変更不可懸念は、共通接続が `row_factory=dict_row` で通常の辞書を返すため不採用。`deferred` は既存集計に対応済み、SQLiteの保持はテスト用途の既知制約。
- [Claude Opus 5（高）](https://claude.ai/chat/254c28eb-62e5-4484-a205-22fee0071378)：BLOCKER 3、WARN 7。指摘は差分外の実装を読めない前提を含むため、独立SEC/品質で実コードと照合して採否を判断した。
  - B1の設定不備によるoutbox試行回数消費は事実として採用。即時の全件消失ではなく8回到達で通知付き打ち切りのため、独立評価はWARN。キュー取得前の接続・列・権限検査を追加した。検査後のDB障害は依然起こり得る。
  - B2の同一ロック再入は実呼び出し経路に存在しない。ロック内は `_remember_row_locked()` を使う。B3の明示unlock必須は、共通接続が新しい直接 `psycopg.Connection` を返し、closeでセッション終了するため不採用。正常・例外後の解放は実Postgresで確認済み。
  - W1の競合判定への波及は `conflict_resolver` がmapping/last_synced_atを参照しないため不採用。W2は安全な固定分類を追加。元例外の全文出力は秘密情報を含み得るため採用しない。
  - W3/W4は受理未完了の調査SQLと接続数の配備前条件を追記。自動監視・接続数の本番実測は未実装/未検証。W6は暗黙ホストを拒否する修正を採用。独自名称の接続プールは運用確認が必要。
  - W5の差し戻し失敗を握ってdeferredとする案は、保存できていないのに成功に見せるため不採用。W7の `<` は新規mapping自身の時刻を通すために必要。同時刻の別イベントは既知の保証対象外で、理由をコードに追記した。
- 他社レビュー本文は `/private/tmp/crm-freshness-{gemini,claude}-review.txt` に一時保存。追加修正版の他社再承認は取得していない。追加差分は独立SEC・品質・QAで再確認済み、BLOCKER 0 / WARN 0。

## 外部レビュー反映後の検証

- 全pytest2,774件成功（27.79秒）。既存のhttpx非推奨警告1件。
- 専用Postgresの追加6項目成功。事前検査成功、テーブル未配備、SELECTのみの権限、読み取り専用接続でキュー取得0回・既存pending/attempts0維持、暗黙/空/大文字poolerホスト拒否、設定復旧後の検査成功を確認。
- 共通接続変更の影響範囲として、初回のロック・watermark実DB11項目も再成功。前回の独立18項目のうち変更と無関係な外部API偽装試験を全再実行したわけではない。
- 読み取り専用試験の初回は接続optionsが既存共通関数で上書きされ、意図した条件にならなかった。専用QAロールのdefault_transaction_read_onlyで条件を作り直し、拒否とキュー無変更を確認した（製品変更なし）。
- 独立SEC・品質の再レビューもBLOCKER 0 / WARN 0。本番は未配備・未操作。
