# CRM住所・電話二次照合の本番配備・実測（2026-09-12）

本番コードは `98f6f756fe8853987762ab170389e69e8f4d6b6b` に反映済み。
通常巡回を前倒しで6回実行し、20:41 JSTに **102,942件すべての二次項目取得が完了、未取得0件**。
削除は全6回とも0件。全件取得後の鳥取県30施設は、既存1・新規候補3・要確認26・未突合0となり、
画面・CSV・実行履歴が一致した。住所だけで候補が見つかった9施設も新規から除外された。
原値空欄の全件集計と電話一致の実ケースは未検証であり、キーなし件数で代用しない。

## 本番への反映

本人の「いけ」を本番配備・実測の指示として受け、dashboard → APIの順に実施した。

| 対象 | 配備ID | 状態 |
|---|---|---|
| dashboard | `dpl_BrhziWiGZ59gW5UMyNZg9VNnmehN` | READY・本番URL割当済み |
| API | `dpl_bTTW2NLbMnSLqt2Ve7iwRsp6tAg4` | READY・本番URL割当済み |

- [本番リスト作成画面](https://crm-sfa-integration-dashboard.vercel.app/facility-list)
- [dashboard配備](https://vercel.com/cnctor/crm-sfa-integration-dashboard/BrhziWiGZ59gW5UMyNZg9VNnmehN)
- [API配備](https://vercel.com/cnctor/crm-sfa-integration/bTTW2NLbMnSLqt2Ve7iwRsp6tAg4)
- Vercel APIの両配備の `githubCommitSha` が対象コミットと完全一致。
- `20260912000000_add_client_identity` は19:31:51 JSTに適用完了。DBの履歴でも成功・巻き戻しなしを確認。
- dashboardはビルド・TypeScript検査成功。APIもビルド成功。製品コードの追加変更はない。
- 未認証アクセスは画面が `/login` へ307、APIのpreview/exportは401。認証保護を維持。

## 本番DBの実測

19:34:31 JSTの読取り結果。接続後に書込み禁止・同一時点読取り・15秒上限を設定した。
ローカル設定に上書きされないVercel production環境でも接続先の一致と件数を照合した。

| 指標 | 実測 |
|---|---:|
| 取引先名インデックス総数 | 102,813 |
| 二次項目取得済み | 0 |
| 二次項目未取得 | 102,813 |
| 住所キーあり | 0 |
| TELキーあり | 0 |
| 取得済み内の両キーあり／住所のみ／TELのみ／両方なし | 各0 |
| 取り込み済み楽天施設 | 30 |

- 最終同期実行は **9月12日04:04:38.704 JST**（DBのUTC値は9月11日19:04:38.704）。実測時点で約15.50時間前、48時間条件は満たす。
- `client_name_index_completed` はまだ存在しない。既存の `client_name_index.updatedAt` を採用した。
- 取得済みが0件なので、取得済みを分母にするキー充足率は算出できない。
- **原値が空欄の取引先が102,813件ある、という意味ではない。** すべて未取得である。
  `ClientNameIndex` は原値を保存せず、キーのNULLには空欄・正規化不採用も含む。
  他の現行DBテーブルにも取引先全件の最新住所/TEL原値はなく、原値空欄の全件内訳は未計測。

### 20:05 JSTの再測定と同期登録

本人の続行指示後、20:05:22 JSTに本番DBを再度書込み禁止で集計した。
総数102,813・取得済み0・未取得102,813・住所キー0・TELキー0で変化なし。
最終同期時刻も同じで、経過は約16.01時間。証跡は一時ディレクトリの `recheck.json`。

Vercelの本番プロジェクト設定を実際に読み、定期実行が有効で、
`/api/cron/relation-sync-reconcile` が今回のAPI配備に登録されていることを確認した。
スケジュールはUTCの `0 19 * * *`、次回予定は **9月13日04:00 JST**。
登録確認であり、次回の起動・完走を確認した意味ではない。

現在の巡回は9月8日04:01:39.499 JST開始で、今回の配備より前から継続している。
現存するインデックス行の `syncedAt` と `passStartedAt` を比較すると、
この巡回で取得した行は96,398、まだ取得していない行は6,415だった。
前者にも二次項目は入っていないため、通常巡回による取得には次の一巡も必要になる。
6,415件は現在DBにある行の内訳であり、Notion側の残件数を直接数えた値ではない。
1回の取得速度・全件取得の完了日時は未計測。

また、Notionの取引先マスターの定義を実APIで1回読み、HTTP 200・0.736秒で取得した。
住所は `rich_text`、都道府県は `select`、TELは `phone_number` で、実装の期待する型と3項目とも一致。
証跡は `notion-schema-check.json`。手元の既存認証での定義取得であり、
本番同期の認証成功・個別行の値・原値空欄の全件内訳は確認していない。

### 続行指示後の通常巡回の前倒し実行

本人の「作業を続けろ。同期ってなんだ。動け。動けるものから」を受け、
夜間待ちをやめてVercelの既存 `crons run /api/cron/relation-sync-reconcile` を逐次実行した。
正規の認証・専用Notionクライアント・ロック・時間予算・途中位置保存を維持し、
しおりをSQL等で手動リセットする操作や、専用の全件バックフィルは行っていない。
各回のHTTP 200・DB反映・ロック解放を確認してから次を起動した。

| 起動（JST） | 今回の取込行数（ログ） | 取得済み（DB） | 未取得（DB） | 結果 |
|---|---:|---:|---:|---|
| 20:19:10 | 6,650 | 6,650 | 96,292 | 配備前からの一巡完了・削除0 |
| 20:22:12 | 17,851 | 24,501 | 78,441 | 新しい一巡を開始・正常中断・削除0 |
| 20:27:23 | 21,190 | 45,678 | 57,264 | 続きから正常中断・削除0 |
| 20:31:52 | 28,186 | 73,789 | 29,153 | 続きから正常中断・削除0 |
| 20:35:41 | 27,817 | 101,459 | 1,483 | 続きから正常中断・削除0 |
| 20:40:14 | 8,244 | 102,942 | 0 | 一巡完了・削除0 |

DB総数は初回実行後に102,813から102,942へ129件増加した。
取込行数には区間境界の再取得が含まれるため、ログの行数を足して取得済み総数にしない。
初回の一巡完了時に通常処理がしおりを解消し、次の起動で新しい一巡を始めた。
取引先名が空のページをスキップした警告はあるが、DBの既存行を削除した警告ではない。

取り込み途中にも同条件でCSVを再実行した。
「問わない」は30件＝要確認24＋未突合6、HTTP 200・6.701秒。
「新規のみ」は出力0件。対象30件の内訳は要確認による除外24＋未突合6、HTTP 200・約9.825秒。
独立QAでCSV本文・34列・BOM・全30行の連絡先等8列非開示を確認した。
この時点の住所候補21施設は全て要確認で、会社別の住所一致根拠が出ていた。
名前だけの段階との変化は、未突合から要確認へ6件、既存から要確認へ1件。
これは部分取得時の結果であり、全行取得後の結果と区別する。

別途試みた手元のNotion100件サンプル取得は未実施。
キーチェーンで認証を取得できず、既存 `config/.env` へ切り替える操作を自動承認レビューが
「未承認の認証元探索」として拒否した。再試行・別の認証探索は行っていない。
この拒否は、既に成功している本番の正規Cron取り込み・ログイン済み画面の検証とは別の操作。

## 全件取得後の実測と画面・CSV確認

20:41:55 JSTのDB読取りでは総数＝取得済み102,942、未取得0。
完了時刻は20:41:10.948 JSTで、最終同期48時間以内の条件を満たす。
通常処理が再開用しおりを解消し、`client_name_index_completed` の完了時刻を記録した。
終了後の取り込みロックは0件。証跡は `after-manual-06.json` と6回分の実行ログ。

| 取得済みの照合用キー | 件数 |
|---|---:|
| 住所・TELともあり | 29,322 |
| 住所のみ | 31,967 |
| TELのみ | 30,405 |
| 両方なし | 11,248 |
| 合計 | 102,942 |

住所キーありは61,289件、TELキーありは59,727件。
**「キーなし」は原値空欄とは異なる。** 空欄以外に、番地不足・内線等で正規化を採用しなかった値も含む。

同じ鳥取県・1室以上・口コミなし含む条件で、本人の画面から最終3種類を実行した。

| CRM条件 | CSV行数 | 既存 | 新規候補 | 要確認（CSV内） | 要確認で除外 | 未突合 | 応答秒 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 問わない | 30 | 1 | 3 | 26 | 0 | 0 | 5.344 |
| 新規のみ | 3 | 0 | 3 | 0 | 26 | 0 | 2.923 |
| 既存のみ | 1 | 1 | 0 | 0 | 26 | 0 | 2.837 |

- 住所候補は27施設・CRMページ計60件。26施設は複数ページ候補（2ページ23施設、4ページ2施設、5ページ1施設）で、全て要確認・住所一致根拠あり。名前候補がなく住所だけで見つかった9施設もこの26施設に含まれ、新規CSVには入らない。
- 名前と住所が同じ1ページを指す1施設は既存で、CSV根拠は住所一致。実際に二次候補の現行Notion値確認を通る経路である。個々のNotion通信時間は計測していない。
- 新規候補3施設は名前候補・住所候補とも0。全件取得前は未突合として除外され、取得後は「登録情報で一致なし」の根拠とともに新規CSVへ入った。未取引を保証する意味ではない。
- 要確認26行と新規候補3行では連絡先等8列を非開示。電話一致の根拠は0件（楽天施設側に電話番号がない）。全件取得後の「住所だけで1社に一致」の単独候補ケースは存在せず、その特定分岐の実証とは区別する。
- CSVは3本ともBOM付きUTF-8・34列。新規3行・既存1行は「問わない」の対応行と作成日時以外の33列が一致することを独立QAで確認済み。
- 最終3本は画面が実際に受信したAPI応答から `final-any.csv` / `final-new.csv` / `final-existing.csv` へ保存した。Downloadsには配備直後の1本だけがあり、連続ダウンロードの保存成功とは扱わない。ブラウザ設定は変更していない。
- 応答観測用のfetchラッパーは復元、ページ内観測データは削除済み。画面証跡は `final-new-screen.png` / `final-existing-screen.png`。

本人の書き出し履歴は追加5件（部分取得中2・取得完了後3）があり、
全件のユーザー・鳥取県条件・API返却件数と一致。今回の総履歴は初回4件と合わせて9件。

| 時刻（JST） | 状態・条件 | CSV件数 | 実行ID |
|---|---|---:|---|
| 20:26:23 | 部分取得・問わない | 30 | `96cd3e74e9b949f19163e3767c7a13dd` |
| 20:30:15 | 部分取得・新規のみ | 0 | `c9080ade1fd94ff3b1c58930082b3f9b` |
| 20:42:26 | 全件取得・問わない | 30 | `749b96a95745406aa2f1641683e645e1` |
| 20:43:01 | 全件取得・新規のみ | 3 | `a95ea2b7fc6f4528921d1dc5ece121e2` |
| 20:43:49 | 全件取得・既存のみ | 1 | `062c717d702f4b8e8bf8729b030d4006` |

独立QAは最終CSV3本・画面2枚・実行履歴5件・DB集計を照合し、追加BLOCKER 0。
製品コード変更・再配備はなく、今回の確認は本番実測とその保存証跡の照合である。

## 配備直後の画面・CSV・実行履歴（19:44〜19:50）

Browser Useで本人の既存Googleアカウントからログインし、本番画面で操作した。
条件は鳥取県、1室以上、口コミなしを含む、商材除外なし、チェーン除外なし、件数上限なし。
「件数を見る」はHTTP 200・30件・16列で、CRMを照合していない旨の画面表示も確認。

| CRM条件 | CSV行数 | 既存 | 新規候補 | 要確認（CSV内） | 未突合（CSV内） | 所要秒 |
|---|---:|---:|---:|---:|---:|---:|
| 問わない | 30 | 1 | 0 | 17 | 12 | 9.877 |
| 新規のみ | 0 | 0 | 0 | 0 | 0 | 11.311 |
| 既存のみ | 1 | 1 | 0 | 0 | 0 | 10.685 |

所要時間はブラウザのリクエスト開始から応答受信完了まで。大規模件数の性能保証ではない。
新規のみ・既存のみの画面には、どちらも「要確認のため除外17件」「未突合12件」と表示された。
未突合件数をCSV行数へ単純に足さない。新規のみでは既存1件も除外される。

- 3種類のCSVはBOM付きUTF-8・34列。末尾は「CRM照合根拠」。独立QAがCSVパーサーで実際に数え、製品の列定義・画面メッセージと一致を確認。
- 要確認17行＋未突合12行の全29行で、担当・電話・FAX・メール・担当者名・役職・現状の取引・提案済みサービスの8列が空欄。
- 新規CSVはヘッダーのみ。既存CSVの1行は「問わない」の既存1行と作成日時以外の33列が一致。
- 照合根拠があるのは既存1行の名前一致だけ。住所一致0行・電話一致0行。要確認17行の根拠欄は空欄であり、**住所単独候補の除外を本番確認したという意味ではない**。
- 新規と既存再確認ではHTTP応答のCSV本文を保存・解析。「問わない」は実際にダウンロードされたCSVを解析した。ブラウザへの連続ダウンロード3本の保存成立は確認していない。
- ブラウザ接続の切替後に応答本文を回収できない場面があったため、既存のみを1回追加実行し、実応答を保存して同じ結果を確認。観測用のfetchラッパーは復元し、保存用のページ内データも削除した。
- Vercelのリクエストログでもpreview 1回・export 4回のHTTP 200を確認。Notion個別通信のログは取得したログにはなく、その通信単位の実時間・成功数は未計測。`contacts_available=true`だけで実Notion取得成功と断定しない。

書き出し履歴は本人のユーザーで次の4件が存在し、条件・件数が画面およびCSVと一致した。

| 時刻（JST） | 条件 | 出力件数 | 実行ID |
|---|---|---:|---|
| 19:44:53 | 問わない | 30 | `2262edca801e4573abdbbe595ab92fee` |
| 19:47:32 | 新規のみ | 0 | `1293e8429dd644e291264dba9f452c7a` |
| 19:48:23 | 既存のみ | 1 | `19863edf2c97432b87b72d631d04bd8d` |
| 19:50:45 | 既存のみ・CSV保存再確認 | 1 | `80ae45a97a134f5f8bf37e7b69da4c48` |

独立QAは今回の成果物について追加BLOCKER 0。実装時のテストを再実行した結果ではない。
一時証跡は `/private/tmp/crm-secondary-production-20260912/` のCSV3本、
`summary.json`、`audit.json`、画面画像2枚。顧客データを含み得るためリポジトリには入れない。

## 残る確認と維持した制約

- 全件取得・鮮度・キー内訳の実測は完了。夜間同期待ちや再配備承認待ちとして止める状態ではない。
- 原値空欄の全件集計は未実施。キーなしの件数で代用しない。手元の追加サンプル取得は上記の認証元利用に対する自動承認レビューで拒否された。
- 電話一致と、全件取得後の住所単独1社候補の分岐は実例がなく未検証。住所だけで見つかる複数ページ候補の除外、名前＋住所の既存判定、無一致の新規候補判定は実データで確認した。
- 26施設で複数CRMページ候補があることは実測したが、同一施設の重複登録か別事業者の同居かは未判定。今回の配備検証で統合・削除しない。
- しおりの手動変更・専用全件バックフィル・他県取り込み・電話番号の追加収集は未実施。既存Cronの手動起動は本人の続行指示後に6回実施し、通常処理によるしおり保存・一巡完了時の解消は正常に発生した。定期スケジュールは変更していない。
- Chrome公式連携は接続一覧が空。公式診断4本は通過した。前回の使用承認を継承したBrowser Useでは接続できた。Chrome再起動・連携設定の修正は行っていない。

実装・判定条件の正本は [設計書§13](facility_list_design.md#13-crm住所電話の二次照合2026-09-12-主機cx)。
